defaults: &defaults
  host: localhost
  isql: isql
  port: 1111
  user: dba
  password:
  base_url: http://togovar.biosciencedbc.jp
  load_dir: /data/togovar

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
  host: virtuoso
