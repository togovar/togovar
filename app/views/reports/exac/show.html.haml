= content_for :header do
  - Stanza.exac.nanostanza.each do |stanza|
    = stanza_link_tag(stanza.name)

  - Stanza.exac.togostanza.each do |stanza|
    = stanza_link_tag(stanza.name)

%h1 ExAcReport#show

.container-fluid
  #nav-wrapper
    #navbar.navbar
      .navbar-inner
        %ul.nav
          - Stanza.exac.togostanza.each do |stanza|
            %li= link_to stanza.name, "##{stanza.name}"

  #header.tg-report-header
    %p.tg-report-type ExAC report
    %h1
      %span#tg-title-id #{@clin_var_id}
      %span#tg-title-label

  - Stanza.exac.togostanza.each do |stanza|
    %section{id: stanza.name}
      .page-header
        %h2= stanza.label
      = stanza.tag(@params)
